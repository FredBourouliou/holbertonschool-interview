# Find the loop

Finds the loop in a singly linked list using Floyd's cycle detection algorithm.

## Prototype

```c
listint_t *find_listint_loop(listint_t *head);
```

## Compilation

```bash
gcc -Wall -pedantic -Werror -Wextra 0-main.c 0-find_loop.c lib.c -o main
```
